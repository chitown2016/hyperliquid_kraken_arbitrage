to install virtual environment use the following code:
conda create --prefix ./env python=3.8.19
Then activate the environment:
conda activate ./env
Then install the requirements:
pip install -r requirements.txt
